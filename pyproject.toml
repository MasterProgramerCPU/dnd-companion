[project]
name = "dnd-companion"
version = "0.1.0"
description = "LAN table companion for in-person D&D 5e games"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "websockets>=12",
    "qrcode>=7.4",
    "python-multipart>=0.0.9",
]

[tool.uv]
package = false
